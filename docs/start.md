This file will detail the process of developing the application which is described in this folder.

### 1) Setup
* all parts of the application need to be properly containerized with Docker
* both backend and frontend should be entirely in TypeScript
* setup a git repo
* setup the technology stack:
  * frontend - ShadCN UI
  * backend:
    * auth.js for authentication (unless a better and simpler option exists)
    * Convex database